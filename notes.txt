# 1. Finalize changelog
# 2. Commit to dev
git add .
git commit -m "chore: finalize v1.0.0"

# 3. Promote to main
git checkout main
git merge dev --ff-only

# 4. Tag
git tag -a v1.0.0 -m "Stable release: OLED clock with temp slideshow"

# 5. Update clean
git checkout clean
git merge main --ff-only

# 6. Push all
git push origin main clean --tags